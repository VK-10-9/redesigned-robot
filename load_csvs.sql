-- DEPRECATED: This helper has been moved to `scripts/postgres_deprecated/load_csvs.sql` for archival purposes.
-- The repository now defaults to CSV-only mode and the Python loader (`scripts/load_dataset.py --dry-run`) is the recommended workflow.

-- If you still need the original Postgres helper, see `scripts/postgres_deprecated/load_csvs.sql`.
